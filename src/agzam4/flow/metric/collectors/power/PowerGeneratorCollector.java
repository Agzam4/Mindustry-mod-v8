package agzam4.flow.metric.collectors.power;

import agzam4.flow.metric.collectors.BaseCollector;
import mindustry.world.blocks.power.PowerGenerator;
import mindustry.world.blocks.power.PowerGenerator.GeneratorBuild;

public class PowerGeneratorCollector<T extends PowerGenerator, B extends GeneratorBuild> extends BaseCollector<T, B> {

	
}
