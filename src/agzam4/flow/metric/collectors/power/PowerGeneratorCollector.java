package agzam4.flow.metric.collectors.power;

import agzam4.flow.metric.collectors.BlockCollector;
import mindustry.world.blocks.power.PowerGenerator;
import mindustry.world.blocks.power.PowerGenerator.GeneratorBuild;

public class PowerGeneratorCollector<T extends PowerGenerator, B extends GeneratorBuild> extends BlockCollector<T, B> {

	
}
